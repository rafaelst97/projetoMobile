<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/home_noticias"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.fragment.NoticiasFragment">

    <!-- TODO: Update blank fragment layout -->

    <ImageView
        android:id="@+id/icone_noticias"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:layout_marginLeft="20dp"
        android:layout_marginTop="20dp"
        app:srcCompat="@drawable/ic_action_noticias"
        tools:src="@tools:sample/avatars" />

    <TextView
        android:id="@+id/exemplo_noticia"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="250dp"
        android:layout_marginLeft="25dp"
        android:elegantTextHeight="true"
        android:textColor="@color/black"
        android:textAppearance="@style/TextAppearance.Material3.BodyLarge"
        android:textSize="30dp"
        android:text="Este é um exemplo de notícia" />

</FrameLayout>